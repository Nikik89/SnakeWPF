<Window x:Class="SnakeWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SnakeWPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        Background="Black"
        WindowStartupLocation="CenterScreen"
        PreviewKeyDown="Window_StartKeyDown"
        KeyDown="Window_KeyDown">
    <Grid x:Name="MainGrid">
        <UniformGrid x:Name="playerGrid"
                     Background="#5F2580" Margin="25,50,450,50"/>
        <UniformGrid x:Name="enemyGrid"
            Background="#300571	" Margin="450,50,25,50"/>
        <Border x:Name="Overlay"
                Background="Gray" Margin="104,72,104,71"
                RenderOptions.EdgeMode="Aliased">
            <TextBlock x:Name="OverlayText"
                       Text="PRESS ANY KEY TO START"
                       FontSize="24"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       />
        </Border>
        <StackPanel>
            <TextBlock x:Name ="Text" Margin="356,69,356,340" Background="White"/>
        </StackPanel>
    </Grid>

</Window>
